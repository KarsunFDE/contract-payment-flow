"""
app.workflow — the M3 agent workflow (LangGraph StateGraph).

Net-new package for ADR-0005 Phase 2 / ADR-0006: the SF-30 modification
workflow (inner graph) and the multi-agent triage flow (outer graph). See
docs/specs/m3.md and docs/tasks/m3-agent-workflow-task-split.md.
"""
