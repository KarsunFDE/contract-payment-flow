# Shell scripts must keep LF endings so they run inside Linux containers,
# regardless of the developer's core.autocrlf setting (Windows checkouts
# would otherwise rewrite these to CRLF and break the shebang). Finding 4.
*.sh text eol=lf
scripts/entrypoint.sh text eol=lf
