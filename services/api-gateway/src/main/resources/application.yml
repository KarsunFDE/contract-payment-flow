server:
  port: 8080

spring:
  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          # In dev we use a local mock issuer; in prod this points at the
          # production OIDC issuer. The JWKS is fetched from $JWT_ISSUER_URI/.well-known/jwks.json
          issuer-uri: ${JWT_ISSUER_URI:http://localhost:8090/auth}

# DEV-ONLY: when true, the gateway permits the SPA's data routes + enables CORS
# for the local Angular origin, because the local stack runs no OIDC issuer.
# Defaults false → production requires a verified JWT (anyExchange().authenticated()).
gateway:
  dev-no-auth: ${GATEWAY_DEV_NO_AUTH:false}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  # ⚠ DELIBERATE — Item 6: api-gateway logs X-Request-ID (other services use different keys).
  pattern:
    console: "%d{HH:mm:ss.SSS} %-5level [X-Request-ID=%X{X-Request-ID:-}] %logger{36} - %msg%n"
  level:
    com.karsunfde.contractflow: INFO
    org.springframework.security: INFO
