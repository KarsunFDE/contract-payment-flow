# Smoke-test report — contract-payment-flow (Pair 2)

> Generated by `pair-brownfield-generator` Step 7 on 2026-05-24.

## Mode

**STATIC** — Docker daemon unavailable in this generator run; substituted with
file-presence + grep-signature inspection per task instructions. Equivalent to
the inspection commands a cohort would run from `docs/debt-inspection.md`.

A live `docker-compose up` smoke test should be re-run by the pair after they
join the repo on W1 Thu morning (cohort host has Docker available).

## Structure check

- 4 services present: `api-gateway/`, `contract-modification-service/`,
  `invoice-review-service/`, `ai-orchestrator/`
- `pom.xml` present in all 3 Java services
- `requirements.txt` + Dockerfile present in `ai-orchestrator`
- `infra/docker/docker-compose.yml` enumerates 8 service blocks (4 app + 4 infra: mongo, postgres, redis, otel-collector pattern from acquire-gov)
- `frontend/` Angular SPA scaffold intact

## Java package-decl integrity

For every `*.java` under `src/main/java/`, verified `package` declaration
matches the on-disk directory path. Zero mismatches — Java compilation will
not fail on package/dir alignment.

## Baseline brownfield-debt detectability (12 / 12 INTACT)

| # | Item | Signature | Status |
|---|------|-----------|--------|
| 1 | JWT signature-skip | `JwtSignatureSkipFilter.java` present in api-gateway | INTACT |
| 2 | Audit-log race | `recordAsync` calls in `AuditLogger.java` | INTACT |
| 3 | No circuit breaker | `ContractModificationClient.java` in invoice-review-service, no `@CircuitBreaker` annotation | INTACT |
| 4 | No `response_model` in ai-orchestrator | 4 endpoints without Pydantic models on `main.py` | INTACT |
| 5 | LangChain v0.x `LLMChain` | `legacy_chain.py` still imports + uses `LLMChain` (11 hits) | INTACT |
| 6 | Inconsistent correlation-IDs | 4 services log differently-named IDs (X-Request-ID / correlationId / traceId / nothing) | INTACT |
| 7 | `pinecone-client` unused | `requirements.txt` line present, no Python file imports it | INTACT |
| 8 | Hardcoded localhost URL | `frontend/.../contract-modification-list.component.ts` has `http://localhost:8081` | INTACT |
| 9 | No `Jsoup.clean` on description | `ContractModificationService.create()` stores `description` verbatim — no `Jsoup` import | INTACT |
| 10 | No multi-tenant `agencyId` filter | `listAll()` calls `repo.findAll()` directly | INTACT |
| 11 | Dockerfile `:latest` tags | 5 of 5 Dockerfiles tagged `:latest` (4 Java + 1 frontend; ai-orchestrator pinned to `python:3.11-slim` as the curriculum-noted exception) | INTACT |
| 12 | GHA workflow lint disabled | Both `.github/workflows/ci.yml` + `infra/github-actions/ci.yml` show commented-out lint step | INTACT |

## Pair-unique brownfield-debt detectability (5 / 5 PRESENT)

| id | Signature | Status |
|----|-----------|--------|
| `sec-bcrypt-rounds-too-low` | `new BCryptPasswordEncoder(4)` in `UserService.java` | PRESENT |
| `rel-bulk-endpoint-no-pagination` | `repo.findAll();` in `ContractModificationListController.listAll()` (no `Pageable`) | PRESENT |
| `obs-metric-unbounded-cardinality` | `"user_id", userId` label arg in `ContractModificationMetrics.recordReview()` | PRESENT |
| `ai-prompt-template-user-controlled` | `f"""...{user_input}..."""` in `contract_modification_prompts.build_draft_prompt` | PRESENT |
| `obs-healthcheck-always-200` | `Map.of("status", "UP")` hardcoded in `HealthController.health()` | PRESENT |

## Locked-failing tests (10 total — 5 baseline + 5 pair-unique)

Baseline (carried in from acquire-gov):
- `services/ai-orchestrator/tests/test_legacy_chain_debt.py` (Item 5)
- `services/ai-orchestrator/tests/test_structured_output_debt.py` (Item 4)
- `services/ai-orchestrator/tests/test_unused_deps_debt.py` (Item 7)
- `services/contract-modification-service/src/test/java/.../InputSanitizationDebtTest.java` (Item 9)
- `services/contract-modification-service/src/test/java/.../MultiTenantBoundaryDebtTest.java` (Item 10)

Pair-unique (D-059 / cohort_1_pair_2_contract):
- `services/contract-modification-service/src/test/java/.../BcryptRoundsTooLowDebtTest.java`
- `services/contract-modification-service/src/test/java/.../HealthcheckAlways200DebtTest.java`
- `services/invoice-review-service/src/test/java/.../BulkEndpointNoPaginationDebtTest.java`
- `services/invoice-review-service/src/test/java/.../MetricUnboundedCardinalityDebtTest.java`
- `services/ai-orchestrator/tests/test_prompt_template_user_controlled_debt.py`

All carry `@Tag("brownfield_debt")` (Java JUnit5) or
`@pytest.mark.brownfield_debt` (Python pytest). All Java tests excluded from
default `mvn test` via existing `excludedGroups>brownfield_debt</excludedGroups>`
surefire config; Python tests excluded via existing pytest-ini marker config.

Expected behavior on a live run:
- `cd services/contract-modification-service && mvn -B test -Dgroups=brownfield_debt` → 4 failures (Item 9, Item 10, BcryptRoundsTooLow, HealthcheckAlways200)
- `cd services/invoice-review-service && mvn -B test -Dgroups=brownfield_debt` → 2 failures (BulkEndpointNoPagination, MetricUnboundedCardinality)
- `cd services/ai-orchestrator && pytest -m brownfield_debt` → 4 failures (Items 5, 4, 7, ai-prompt-template-user-controlled)

Inverted-lockfile convention: green CI = all debt items still failing (intact).
At W4-W5 fix time, cohort flips entries in `docs/debt-lockfile.yml` from
`locked: true` → `locked: false` per the debt-touch-approved PR label.

## Result

PASS (static mode) — all 12 baseline + 5 pair-unique debt signatures are
present, all 10 locked-failing tests are wired in, package decls align with
on-disk paths. Pair may proceed on W1 Thu morning.

## Follow-up for the pair

1. Run `docker-compose up -d` in the cohort env and confirm 5 services boot.
2. Run the 3 test commands above and confirm 10 total failures (all expected).
3. Read `docs/pair-unique-debt.md` together — these 5 items are yours alone.
4. Read `BACKLOG.md` — pick stretch items to defend in W3/W6 retro.
5. Open `docs/adrs/0001-post-award-contract-administration-commitment.md` —
   countersign + add your pair members' names if instructor hasn't already.
